<div class="navbar navbar-default navbar-fixed-top  col-xs-12 col-md-12 col-sm-12" id="navbar" role="navigation">
    <div class="w1200 bc">
        <ul class="nav navbar-nav">
            <li>
                <a class="brand" href="/products"><img src='<%= image_url 'hunter.png' %>' height="50" width="150" ></a>
            </li>
            <!--===商品分类===-->
            <li>
                <a href="/products?category=课程" style="<%= if !params[:where].blank? && params[:where]=="课程" then "color:#B28435;" end%>"><%= t('course') %></a>
            </li>
            <li>
                <a href="/products?category=工具" style="<%= if !params[:where].blank? && params[:where]=="工具" then "color:#B28435;" end%>"><%= t('tool') %></a>
            </li>
            <!--下拉菜单-->
            <li>
                <%= link_to( t("Hunters"), href="https://floating-earth-63565.herokuapp.com") %>
            </li>
        </ul>
        <div class="navbar-form navbar-left">
            <%= form_tag search_products_path, :method => :get do %>
                <div class="input-group">
                    <input type="text" class="form-control" name="q" value="<%= params[:q] %>" placeholder="寻宝">
                    <div class="input-group-btn">
                        <button class="form-control" type="submit">
                            <i class="glyphicon glyphicon-search"></i>
                        </button>
                    </div>
                </div>
            <% end %>
        </div>
        <ul class="nav navbar-nav navbar-right">
            <!-- 收藏标签链接 -->
            <li><%= link_to(t('favorite'), products_path(:favorite => "yes"))%> </li>
            <!-- 语系下拉菜单 -->
            <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= t("language") %><span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <!-- 语系切换-->
                    <li><%= link_to "简体中文", :controller => controller_name, :action => action_name, :locale => "zh-CN" %></li>
                    <li><%= link_to "繁體中文", :controller => controller_name, :action => action_name, :locale => "zh-TW" %></li>
                </ul>
                <!-- 愿望清单(购物车功能)-->
                <li >
                    <%= link_to carts_path do  %>
                        <%= t('wish-list') %> <img src='<%= image_url 'cart.png' %>'height="18" width="18" >( <%= current_cart.products.count %> )
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
