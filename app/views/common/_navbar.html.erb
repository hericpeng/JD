
<nav class="navbar navbar-default navbar-fixed-top col-xs-12 col-md-12 col-sm-12" id="navbar" role="navigation">
    <div class="container-fluid col-md-12">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <!-- <a class="navbar-brand" href="/">猎人学院</a> -->
            <a class="navbar-brand" href="/"><img src='<%= image_url 'hunter.png' %>' height="50" width="150" ></a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-8">
            <ul class="nav navbar-nav">
                <!-- <li >
                    <a href="/products/search?utf8=✓&amp;q=知识宝藏">课程</a>
                </li>
                <li >
                    <a href="/products/search?utf8=✓&amp;q=效率工具">工具</a>
                </li> -->
                <!-- 商品分类 -->
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">分类浏览
                    <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <% Category.all.each do |category| %>
                            <li>
                                <%= link_to(category.name, products_path(category: category.name)) %>
                            </li>
                        <% end %>
                    </ul>
                </li>
                <li>
                    <%= link_to("知识猎人公会", href="https://floating-earth-63565.herokuapp.com") %>
                </li>
            </ul>
            <div class="navbar-form navbar-left">
                <%= form_tag search_products_path, :method => :get do %>
                    <div class="input-group">
                        <input type="text" class="form-control" name="q" value="<%= params[:q] %>" placeholder="寻宝">
                        <div class="input-group-btn">
                            <button class="form-control" type="submit">
                                <i class="glyphicon glyphicon-search"></i>
                            </button>
                        </div>
                    </div>
                <% end %>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <%= link_to carts_path do  %>
                        愿望清单 <img src='<%= image_url 'cart.png' %>'height="18" width="18">( <%= current_cart.products.count %> )
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
